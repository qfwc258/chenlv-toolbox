# 陈律工具箱 · Android 应用

把 Markdown 文件一键转换成符合党政机关公文格式（GB/T 9704 风格）的 Word 文档；支持给已有 PDF 批量添加页码；并内置一个**轻量 Markdown 编辑器**，可直接打开、编辑、保存，还能一键把当前内容转成党政公文 Word。

## 安装

把 `陈律工具箱.apk` 传到手机，点击安装。若提示「未知来源」，在系统设置里允许即可。

- 最低系统：Android 7.0（API 24）
- 已在 arm64 / armv7 / x86 全架构通用（纯 Java/Kotlin 实现，无 native 依赖）

## 界面结构

App 采用**居中顶栏 + 底部导航**的经典布局，整体配色为公文红/米色系：

- **顶栏**：法槌图标 +「陈律工具箱」标题 + 动态副标题（当前模式说明）
- **底部导航**：三个标签页，各司其职，互不重复，单页内容都很短：

| 标签 | 职责 |
| --- | --- |
| **文档** | 打开 / 编写 / 保存 Markdown，一键生成公文 |
| **公文** | 公文预览、点字就地编辑、导出 Word / PDF |
| **PDF 页码** | 给已有 PDF 批量加页码 |

启动时会根据文件类型自动切换：

- 打开 / 分享 `.pdf` → 自动进入「PDF 页码」
- 打开 / 分享 `.md`、`.txt` 等文本 → 自动进入「文档」

## 文档（编辑 + 一键转公文）

「文档」标签是一个轻量 Markdown 编辑器，且与公文工作流无缝衔接——编辑好的内容可直接转成 Word，无需在多个标签页之间来回切换。

1. 点工具栏「打开」，从任意位置（手机存储、QQ/微信下载目录、网盘）选择 `.md` 等文本文件
2. 在编辑区直接撰写 / 修改；右上「眼睛」按钮可切换**实时预览**，按公文结构（标题层级、引用、列表、落款对齐等）查看效果；「字号」`−/+` 调整等宽字体大小，未保存时文件名旁会显示橙色圆点
3. 点「保存」：
   - 若文件有写权限，**直接写回原文件**；
   - 同时总会**另存一份副本**到 **下载 / 陈律文档/**（如 `草稿.md`），确保文件不丢失
4. 展开底部「公文输出」面板，按需设置**智能引号**、**主标题字体**，点「转换为党政公文 Word」
5. 生成的 docx 自动保存到 **下载 / 陈律文档/** 目录（文件名沿用原文件名，如 `报告.docx`），结果卡片可直接「打开」或「分享」

也支持从其他 App 直接调起：在文件管理器里长按 md / txt 文件 →「打开方式」或「分享」→ 选择 陈律工具箱。

## 公文（预览 · 编辑 · 转 PDF）

独立的第二个标签页，有**两种进入方式**：

1. **无缝对接**：在「文档」页点【生成公文】，Word 存好后**自动跳到这里**预览；
2. **单独使用**：直接切到「公文」页，点【打开文件排版成公文】，选一个 md / txt 就能排版预览，不必先去「文档」页。

页面能力：

- **预览**：按公文版式（标题居中、正文首行缩进、落款右对齐、表格框线）渲染成近似纸张的白色页面，所见即所得地核对排版。
- **就地编辑**：点任意段落或表格单元格，弹出输入框改文字，改完立即反映到预览与后续导出。
- **存 Word**：把当前（可能已编辑过的）公文重新序列化为 `.docx`。
- **转 PDF**：渲染为 PDF（矢量文字、清晰、体积小），保存到 **下载 / 陈律文档/**（文件名沿用主标题，如 `报告.pdf`）。

底部固定一行操作栏 `[存 Word] [转 PDF] [分享]`，导出后自动弹出结果框，可「用其他应用打开」或「分享文件」。
信息条右侧的 `×` 可关闭当前公文，回到空状态重新选文件。

> 「公文」标签有内容时，底部导航图标上会出现一个小红点提示。

> 预览 / 编辑 / 导出均基于同一份「公文模型」，与 Word 输出保持一致的版式语义；PDF 使用系统字体（通常为思源 / Noto），在电脑 Office / WPS 中打开即为标准公文样式。

## PDF 加页码

切到「PDF 加页码」标签页，点「选取 PDF 文件」：

1. 设置页码选项：
   - **页码位置**：底部居中 / 居左 / 居右，顶部居中 / 居左 / 居右
   - **起始页码**：从几开始（如第 1 页显示 1）
   - **字号 (pt)**：页码文字大小，默认 4
   - **边距 (mm)**：页码距页边的距离，默认 10
   - **页码颜色**：黑 / 红 / 蓝 / 绿 / 紫 / 灰（圆形色块选择）
   - **前缀（可选）**：在数字前加文字，如「第」→ 显示「第1」「第2」…
2. 点「添加页码」
3. 处理完成后，点结果卡片上的「打开文件」即可**直接查看**生成的 PDF（最可靠）；
   同时文件也会保存到 **下载 / 陈律文档/** 目录（文件名沿用原文件名加 `_页码` 后缀，如 `证据_页码.pdf`），
   可在文件管理器中按文件名查找，或用「分享文件」发给他人。

也支持从微信 / 文件管理器等直接分享 PDF → 选择 陈律工具箱，会自动进入加页码模式。

> 注意：
> - 极少数「真正设了打开密码」的 PDF 仍无法处理，App 会**明确弹窗提示真实错误**（不再误报加密）；
>   请用电脑上的 PDF 阅读器打开后「另存为」导出无密码版本，再导入本应用。
> - 超大文件（>30 MB）会先弹出确认提示，处理时显示**全屏加载遮罩**，请耐心等待不要退出。
> - 纯本地处理，文件不会上传任何服务器。

### 关于清晰度（重要）

加页码**不会降低原 PDF 的清晰度**，采用双引擎保障：

1. **首选 PDFBox 无损方案**：直接在每页内容流末尾叠加页码文本，原页面（矢量线条 / 文字 / 嵌入图片）**原样保留，清晰度 100% 不变**，且文字仍可选中、文件体积几乎不增加。
2. **原生渲染兜底方案**（仅当 PDFBox 因特殊结构 / 解析异常失败时才启用）：把每页光栅化后画上页码再写出。
   该方案采用 **2.5 倍超采样（≈180dpi）** 渲染，已显著高于默认的 72dpi 栅格化，
   能最大程度保留原图清晰度，不会出现「页面被压缩变模糊」的情况；
   代价是输出为栅格化页面（文字不可再选中）、文件体积偏大。

> 也就是说：**绝大多数正常 PDF 都是无损叠加，完全不降清晰度**；只有极少数 PDFBox 处理不了的「疑难 PDF」才会走栅格化兜底，此时也尽量保真。

实现方式：采用**双引擎**策略——
1. 首选 PDFBox（Android 移植版）：在每页内容流末尾按所选位置叠加 Helvetica 页码文本，**无损**、文字可选中、体积小；
2. 若 PDFBox 失败（加密 / 解析异常 / 内存不足等），自动**回退到 Android 原生渲染引擎**（PdfRenderer + PdfDocument，借鉴 PDF 编辑器开源实现）：先把每页渲染成位图、画上页码再重写，兼容性强，对权限加密、特殊结构 PDF 往往能成功（输出为栅格化页面，文字不可再选中）。

## 排版规则

| Markdown 写法 | 输出格式 |
| --- | --- |
| `# 标题` | 方正小标宋简体 二号(22pt)，居中，后空一行 |
| `## 一级标题` | 黑体 三号(16pt)，首行缩进 2 字符 |
| `### 二级标题` | 楷体_GB2312 三号 |
| `#### 三级标题` | 仿宋_GB2312 三号 |
| `> 引用` | 楷体_GB2312 三号 |
| 普通段落 | 仿宋_GB2312 三号，首行缩进 2 字符 |
| `ll 顶格文字` | 取消首行缩进（用于「各处室：」等主送机关） |
| `---` 之后的内容 | 落款区，整体右对齐 |
| `\| 表格 \|` | 全框线表格，自动等分页宽 |
| `**粗体**` | 加粗 |
| `*斜体*` | 倾斜 |

固定页面规格：

- A4 纸（21 × 29.7 厘米），纵向
- 页边距：上 3、下 2.8、左右各 2.5 厘米
- 固定行距 28 磅，段前段后 0

## 可调选项

- **智能引号**：把英文直引号 `"` 自动配对转成中文弯引号 `“”`
- **主标题字体**：小标宋 / 黑体二选一。部分手机没装小标宋，切黑体可避免显示异常

## 关于字体

生成的 docx 里写入的是字体**名称**（仿宋_GB2312、黑体等），实际显示效果取决于打开设备是否安装对应字体：

- 电脑端 WPS / Office：通常自带，显示完全正确
- 手机端：可能回退到默认字体，属正常现象，把文件传到电脑打开即为标准公文样式

## 实现说明

App 内部不依赖 POI 等重型库，而是直接生成 OOXML（docx 本质是 zip + XML），
因此安装包体积小、转换速度快、且排版细节完全可控。

页面尺寸在 `sectPr` 中显式写入 A4 的 twips 值并标注 `w:code="9"`，
避免部分手机 Office 把文档误判为 Letter 纸张。
