org.gradle.jvmargs=-Xmx3072m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# ============================================================
# 版本号单一来源（app/build.gradle.kts 与 build_apk.sh 共同读取）
# 默认走「提交数驱动」自动递增；VERSION_CODE/VERSION_NAME 可显式覆盖发版。
# 背景：仓库曾做历史瘦身（提交数 199→183），下述偏移用于接续 v3.0.7 并保证
# versionCode 单调递增，请勿在无明确升级规划时改动。
# ============================================================
VERSION_BASE_COMMIT=75
VERSION_MAJOR_BASE=2
VERSION_CODE_OFFSET=17
